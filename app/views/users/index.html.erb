
<%= will_paginate %>
  <ul>
    <%= @users.each do |user| %>
      <li>
        <%= link_to user.name, user %>
      </li>  
    <% if current_user.admin?%> | <%= link_to "delete it", user_delete_path_url(id: user.id), method: :get %>
    <% end %>

    <% end %>
  </ul>
<%= will_paginate %>